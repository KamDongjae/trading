# trading_server.py / trading_client.py / data_collector.py / screener_prepump.py
# 공통 의존성 정리본. 아래만 설치하면 4개 파일 다 돌아간다.
#
# 설치:
#   pip install -r requirements.txt
# Termux(안드로이드)라면:
#   pkg install python python-pandas python-numpy   # 바이너리로 먼저 깔고
#   pip install -r requirements.txt                  # 나머지(pybithumb 등)만 pip로
#   (Termux에서 pandas/numpy를 pip로 바로 빌드하면 시간이 오래 걸리거나 실패할 수 있어서
#    pkg로 먼저 까는 걸 권장. tkinter는 pip 패키지가 아니라 `pkg install tkinter`로 설치)

pandas>=2.0
numpy>=1.24
requests>=2.31
dnspython>=2.4
pybithumb>=1.1

# 아래는 pandas가 내부적으로 쓰는 의존성인데, Termux 환경에서는 자동으로 안 딸려오는
# 경우가 있어서 명시적으로 같이 설치되게 넣어둠.
python-dateutil>=2.8
pytz>=2023.3
tzdata>=2023.3
six>=1.16

# tkinter는 표준 라이브러리 취급이지만 일부 배포판/Termux에선 별도 설치가 필요합니다.
#   Termux : pkg install tkinter
#   Ubuntu/Debian : apt install python3-tk
#   (Windows/macOS 공식 python.org 설치본은 기본 포함)
